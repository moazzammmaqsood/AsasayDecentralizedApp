import { Button, Checkbox, Form, Input } from 'antd';


function CustomButton(){
return(
    <Button type="primary" htmlType="submit" style={{ backgroundColor: "#79C750" }} block>
                            Submit
                        </Button>
);
}

export default CustomButton